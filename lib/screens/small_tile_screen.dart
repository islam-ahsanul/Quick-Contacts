import 'package:flutter/material.dart';

class SmallTileScreen extends StatelessWidget {
  const SmallTileScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('This is small tile screen'),
    );
  }
}
